# Conjugate Gradient Solver Group

This project contains an example of the conjugate gradient (CG) solver group, which solves linear equations using the CG method in a matrix-free manner.

# Usage
```bash
uv run python3 main.py
```
