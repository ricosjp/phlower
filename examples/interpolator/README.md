# Interpolator

This project contains an example of the interpolator preset group.
Inside the model, features are interpolated to evaluate them on the specified geometries, which can differ from the original mesh.

# Usage
```bash
uv run python3 main.py
```
