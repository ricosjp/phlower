
model:

  inputs:
    - name: feature0
      members:
        - name: feature0
          n_last_dim: 10

    - name: feature1
      members:
        - name: feature1
          n_last_dim: 12

  fields:
    - name: support1
      is_time_series: false

  network:
    nn_type: GROUP
    name: CYCLE_MODEL
    
    modules:
      - nn_type: PresetGroup
        preset_type: Identity
        name: MLP0
        inputs:
          - name: feature0
            n_last_dim: 1
        # output_nodes:
          #- name: feature1
          #  dim: 1
        destinations:
          - GCN0
        nn_parameters:
          input_to_output_map:
            feature0: feature1

